# 🚀 Быстрый старт - Система управления меню столовой

## Самый простой способ (Docker)

### 1. Установите Docker
- **Windows/Mac:** [Docker Desktop](https://www.docker.com/products/docker-desktop)
- **Linux:**
  ```bash
  # Arch Linux
  sudo pacman -S docker docker-compose
  sudo systemctl start docker
  sudo systemctl enable docker

  # Ubuntu/Debian
  sudo apt install docker.io docker-compose
  sudo systemctl start docker
  sudo systemctl enable docker
  ```

### 2. Запустите приложение

```bash
cd CanteenMenu
docker-compose up
```

### 3. Откройте браузер

Перейдите на http://localhost:3000

**Готово!** 🎉

---

## Что дальше?

- Просмотрите меню в разделе "Меню"
- Добавьте новые блюда
- Управляйте категориями и ингредиентами
- Фильтруйте блюда по категориям

---

## Остановка

```bash
# Ctrl+C в терминале, затем:
docker-compose down
```

---

## Подключение к базе данных

**Хост:** localhost
**Порт:** 10748
**База данных:** canteen_db
**Пользователь:** postgres
**Пароль:** postgres

```bash
psql -h localhost -p 10748 -U postgres -d canteen_db
```

---

## Полезные команды

```bash
# Запуск в фоновом режиме
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Перезапуск
docker-compose restart

# Полная очистка (включая данные БД)
docker-compose down -v
```

---

## Нужна помощь?

- **Полная документация:** [README.md](README.md)
- **Docker инструкции:** [DOCKER.md](DOCKER.md)
- **Руководство пользователя:** [USER_GUIDE.md](USER_GUIDE.md)
- **Отчет по проекту:** [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md)

---

## Альтернативный запуск (без Docker)

Если Docker недоступен, следуйте инструкциям в [README.md](README.md) раздел "Вариант 2: Локальная установка".

---

**Приятного использования!** 🍽️
