<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700&display=swap" rel="stylesheet">
    <!-- Bootstrap & Custom CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css?v=3">
</head>
<body>
    <nav class="navbar navbar-expand-lg mb-4">
        <div class="container">
            <a class="navbar-brand" href="/">Столовая</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link {{#if (eq activePage 'home')}}active{{/if}}" href="/">Главная</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{#if (eq activePage 'dishes')}}active{{/if}}" href="/dishes">Меню</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{#if (eq activePage 'categories')}}active{{/if}}" href="/categories">Категории</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{#if (eq activePage 'ingredients')}}active{{/if}}" href="/ingredients">Ингредиенты</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{#if (eq activePage 'cart')}}active{{/if}}" href="/cart">
                            Корзина
                            {{#if cartCount}}
                            <span class="badge bg-danger rounded-pill">{{cartCount}}</span>
                            {{/if}}
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{#if (eq activePage 'orders')}}active{{/if}}" href="/orders">Мои заказы</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{#if (eq activePage 'about')}}active{{/if}}" href="/about">О программе</a>
                    </li>
                </ul>
                <!-- Переключатель темы -->
                <button class="theme-toggle" id="themeToggle" aria-label="Переключить тему">
                    <span class="theme-icon">🌙</span>
                </button>
            </div>
        </div>
    </nav>

    <div class="container">
        {{{body}}}
    </div>

    <footer class="mt-5 py-3">
        <div class="container text-center">
            <p class="mb-0">&copy; 2024 Система управления меню столовой</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/theme-toggle.js"></script>
</body>
</html>
